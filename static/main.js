// Basic JS for Employee Attendance System
document.addEventListener("DOMContentLoaded", function() {
    // Example: show a welcome message on dashboard
    if (document.getElementById("dashboard-welcome")) {
        document.getElementById("dashboard-welcome").textContent = "Welcome to the Employee Attendance System!";
    }
});